# Server/deployment secret. Never use the VITE_ prefix for private keys.
OG_PRIVATE_KEY=
OG_RPC_URL=https://evmrpc-testnet.0g.ai

# Safe to expose in the frontend bundle after deployment.
VITE_REGISTRY_ADDRESS=

# Server-only production secrets. Generate with npm run generate:secrets.
ISSUER_PRIVATE_KEY=
ASSESSMENT_SECRET=

# Server-only 0G Compute Router key. Never prefix with VITE_.
OG_API_KEY=
OG_MODEL=qwen2.5-omni
